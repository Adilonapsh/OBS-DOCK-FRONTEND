import { defineTheme } from "../types";
import { defaultTheme } from "../default";

export default defineTheme({
  name: "Dark Glass",
  icon: "🌙",
  category: "core",
  description: "Default dark glassmorphism - balanced for all overlays",
  theme: defaultTheme,
});
